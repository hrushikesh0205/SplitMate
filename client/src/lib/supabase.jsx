/**
 * Supabase has been removed. This file is kept as an empty stub so any
 * residual import paths don't break during the migration. It exports nothing.
 *
 * All auth now goes through src/lib/authService.jsx (Express + JWT).
 * All data calls go through the domain-specific *Service.jsx modules.
 */
